<Window x:Class="OPGslutuppgift.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OPGslutuppgift"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <Label Content="COOKMASTER" FontSize="60" Margin="0,20,0,20" HorizontalAlignment="Center" FontWeight="Bold"/>
            
            <Label Content="Username:" FontSize="20" Margin="0,10,0,0" HorizontalAlignment="Center"/>
            <TextBox Text="{Binding Username, Mode=TwoWay}" Margin="0,0,0,0" Width="100"/>
            <Label Content="Password:" FontSize="20" Margin="0,10,0,0" HorizontalAlignment="Center"/>
            <TextBox Text="{Binding Password, Mode=TwoWay}" Margin="0,0,0,0" Width="100"/>


            <Button Content="Login" Command="{Binding LoginCommand}" Width="50" Margin="0,10,0,0"/>
            <StackPanel Orientation="Vertical" Margin="0,10,0,0">
                <Button Content="Sign Up" Command="{Binding OpenRegisterCommand}" Width="50"/>
                <Button Content="Forgot Password" Command="{Binding ForgotPasswordCommand}" Margin="10" Width="100"/>
            </StackPanel>
        </StackPanel>
        
    </Grid>
</Window>
