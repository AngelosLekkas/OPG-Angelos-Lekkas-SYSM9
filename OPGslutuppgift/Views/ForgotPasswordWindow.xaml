<Window x:Class="OPGslutuppgift.Views.ForgotPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OPGslutuppgift.Views"
        mc:Ignorable="d"
        Title="ForgotPasswordWindow" Height="450" Width="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Reset Password" 
                       FontSize="28" FontWeight="Bold" 
                       HorizontalAlignment="Center" />

            <TextBlock Text="Username:"/>
            <TextBox Text="{Binding Username}" Margin="0,5,0,10"/>

            <Button Content="Find User" Command="{Binding FindUserCommand}" Margin="0,5,0,10" Width="100"/>

            <TextBlock Text="{Binding SecurityQuestion}" Margin="0,10,0,0"/>
            
            <TextBlock Text="Answer:" Margin="0,5,0,0"/>
            <TextBox Text="{Binding SecurityAnswer}" Margin="0,5,0,10"/>

            <TextBlock Text="New password:"/>
            <TextBox Text="{Binding NewPassword}" Margin="0,5,0,10"/>

            <Button Content="Reset" Command="{Binding ResetPasswordCommand}" Width="100" HorizontalAlignment="Center" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
