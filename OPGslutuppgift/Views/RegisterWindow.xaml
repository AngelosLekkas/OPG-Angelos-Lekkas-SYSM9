<Window x:Class="OPGslutuppgift.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OPGslutuppgift.Views"
        mc:Ignorable="d"
        Title="RegisterWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <Label Content="REGISTER" FontSize="40" HorizontalAlignment="Center" Margin="0,20,0,20"/>
            <Label Content="Username:" FontSize="20" Margin="0,0,0,5" HorizontalAlignment="Center"/>
            <TextBox Text="{Binding Username, Mode=TwoWay}" Width="200"/>
            <Label Content="Password:" FontSize="20" Margin="0,0,0,5" HorizontalAlignment="Center"/>
            <TextBox Text="{Binding Password, Mode=TwoWay}" Width="200"/>
            <Label Content="Country:" FontSize="20" Margin="0,0,0,5" HorizontalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding Countries}" SelectedItem="{Binding Country, Mode=TwoWay}" Width="200">
                
            </ComboBox>

            <Button Content="Register" Command="{Binding RegisterCommand}" Width="80" Margin="20"/>
        </StackPanel>
    </Grid>

</Window>
