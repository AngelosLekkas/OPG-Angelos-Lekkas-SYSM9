<Window x:Class="OPGslutuppgift.Views.RecipeListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OPGslutuppgift.Views"
        mc:Ignorable="d"
        Title="RecipeListWindow" Height="450" Width="800">
    <Grid >
        <StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBlock Text="Inloggad som: " FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding ShowCurrentUsername}" Margin="5,0" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="User" Command="{Binding UserCommand}" Margin="0,0,10,0"/>
                <Button Content="Info" Command="{Binding InfoCommand}" Margin="0,0,10,0"/>
            </StackPanel>
            
            <TextBlock Text="Recipes" FontSize="28" HorizontalAlignment="Center" FontWeight="Bold"/>

            <StackPanel Orientation="Horizontal" Margin="10">
                
                <!--search-->
                <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
             Width="150" Margin="0,0,15,0"/>

                <!--category-->
                <TextBlock Text="Category:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox ItemsSource="{Binding Categories}" 
              SelectedItem="{Binding SelectedCategory}" 
              Width="120" Margin="0,0,15,0"/>

                <!--date-->
                <TextBlock Text="Date:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <DatePicker SelectedDate="{Binding SelectedDate}" Width="130" Margin="0,0,15,0"/>

                <!--sort-->
                <TextBlock Text="Sort:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox ItemsSource="{Binding SortOrders}" 
              SelectedItem="{Binding SortOrder}" 
              Width="120"/>
            </StackPanel>

            <ListBox ItemsSource="{Binding Recipes}"
                     SelectedItem="{Binding SelectedRecipe}"
                     DisplayMemberPath="Title"
                     Height="200" Margin="0,10"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="20">
                <Button Content="Add" Command="{Binding AddRecipeCommand}" Width="80"/>
                <Button Content="Details" Command="{Binding DetailsCommand}" Width="80"/>
                <Button Content="Remove" Command="{Binding RemoveRecipeCommand}" Width="80"/>
                <Button Content="Sign Out" Command="{Binding SignOutCommand}" Width="80"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
